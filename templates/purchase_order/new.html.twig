{% extends 'base.html.twig' %}

{% block navbar %}
    <table>
        <tr>
            <td><a href="{{ path('product_index') }}">Výpis produktů</a></td>
            <td>|</td>
            <td><a href="{{ path('purchase_order_index') }}">Výpis objednávek</a></td>
            <td>|</td>
            <td><a href="{{ path('product_new') }}">Nový produkt</a></td>
            <td>|</td>
            <td><b><a href="{{ path('purchase_order_new') }}">Nová objednávka</a></b></td>
         </tr>
    </table>
{% endblock %}

{% block title %}New PurchaseOrder{% endblock %}

{% block body %}
    <h1>Create new PurchaseOrder</h1>

    {{ include('purchase_order/_form.html.twig') }}

    <a href="{{ path('purchase_order_index') }}">back to list</a>
{% endblock %}
